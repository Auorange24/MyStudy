#include <bits/stdc++.h>
using namespace std;


int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if (a > 1) && (c ==0) {
        b = b / a;
    }
    if (a == 1) && (b > 1) {
        b = b + 1
    }
}